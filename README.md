# MiniSIP — Homebrew Tap

Легкий нативний macOS SIP-клієнт.

## Встановлення

```sh
brew tap seishio/minisip
brew install --cask minisip
```

## Оновлення

```sh
brew upgrade --cask minisip
```

## Видалення

```sh
brew uninstall --cask minisip
brew untap seishio/minisip
```
